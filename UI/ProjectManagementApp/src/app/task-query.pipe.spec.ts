import { TaskQueryPipe } from './task-query.pipe';

describe('TaskQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
